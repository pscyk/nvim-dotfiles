-- kanagawa.nvim: rebelot's Hokusai-inspired themes. Provides the
-- kanagawa-wave (default dark), kanagawa-dragon (darker) and
-- kanagawa-lotus (light) colorschemes. Switch with `:colorscheme
-- kanagawa-dragon` etc.; the active default + transparency live in
-- lua/config/theme.lua.
return {
  'rebelot/kanagawa.nvim',
  lazy = false,
  priority = 1000,
}
