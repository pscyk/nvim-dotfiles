-- nightfox.nvim: EdenEast's fox-family themes. Active variant is carbonfox
-- (the darkest, near-black variant). Activated (and transparency applied)
-- in lua/config/theme.lua.
return {
  'EdenEast/nightfox.nvim',
  lazy = false,
  priority = 1000,
}
